[project]
name = "echorelay"
version = "0.1.0"
description = "A simple whatsapp message forwarder, first step before building the entire ai tool"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pytest>=9.1.1",
]
